import styled from "styled-components";

const ShopAppWrapper = styled.div`
  font-family: Georgia, serif;

  .content {
    display: flex;
    width: 100%;
    flex-direction: column;
  }
`;

export { ShopAppWrapper };
